spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///Users/abhgarg6/Abhinav/IntellijWs/spring-cloud-bootstrap
          default-label: master
server:
  port: 8888

eureka:
  client:
    serviceUrl:
      defaultZone: http://netflix-eureka-server.default/eureka