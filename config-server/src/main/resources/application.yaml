spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/abhinavymca/spring-cloud-bootstrap
          default-label: main
server:
  port: 8888

eureka:
  client:
    serviceUrl:
      defaultZone: http://netflix-eureka-server/eureka