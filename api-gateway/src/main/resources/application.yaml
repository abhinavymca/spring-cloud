spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lowerCaseServiceId: true

server:
  port: 8765

eureka:
  client:
    serviceUrl:
      defaultZone: http://netflix-eureka-server/eureka

